version: "2"
linters:
  enable:
    - exhaustive
    - staticcheck
    - unused
    - gocritic
    - misspell
  exclusions:
    paths:
      - node_modules
    rules:
      - path: _test\.go
        linters:
          - errcheck
formatters:
  enable:
    - gofmt
    - goimports
